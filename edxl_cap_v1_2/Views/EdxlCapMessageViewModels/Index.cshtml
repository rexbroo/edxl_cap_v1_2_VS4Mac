@model IEnumerable<edxl_cap_v1_2.Models.ContentViewModels.EdxlCapMessageViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_CapIntroLayout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Alert_Identifier">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Alert_Identifier">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Alert_Identifier">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
