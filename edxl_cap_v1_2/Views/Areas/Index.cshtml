@model IEnumerable<edxl_cap_v1_2.Models.Area>
@using edxl_cap_v1_2.Models

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_CapCategoryLayout.cshtml";
}

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
</head>
<body>

    <h3>Area</h3>

    <style>
        tr:nth-child(even) {
            background-color: lightBlue;
        }

        tr:nth-child(odd) {
            background-color: white;
        }
    </style>

    <table id="elementTable" class="smallText">

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <input type="checkbox" name="start">start
                </td>
                <td>
                    &nbsp;&nbsp;element:<br>
                    &nbsp;&nbsp;@Html.DisplayNameFor(model => model.AreaDesc)
                    @if (Element.Required == true)
                    {
                        <text>*</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }
                </td>
                <td>
                    <div id="elementInput">
                        @if (Element.DatatypeSize < 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.AreaDesc) value
                                <input type="text" name="elementValue" value="@Html.DisplayFor(modelItem => item.AreaDesc)" size="25" />
                            </span>
                        }
                        else if (Element.DatatypeSize > 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.AreaDesc) value
                                <textarea name="elementValue" rows="3" value="@Html.DisplayFor(modelItem => item.AreaDesc)" cols="25">
                                </textarea>
                            </span>
                        }
                    </div>
                </td>
                <td>
                    @if (Element.Repeatable == true)
                    {
                        <input type="radio" name="repeatElement" value="" /><text>&nbsp; repeat</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }
                </td>
                <td>
                    <form asp-area="" asp-controller="areas" asp-action="Details" method="post" asp-route-id="@item.AreaIndex">
                        <input type="hidden"
                               name="elementId"
                               value="${element.id}">
                        <input type="submit"
                               value="add element">
                    </form>
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.AreaIndex">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.AreaIndex">Delete</a>
                </td>
            </tr>
            <tr>
                <td>
                    <input type="checkbox" name="start">start
                </td>
                <td>
                    &nbsp;&nbsp;element:<br>
                    &nbsp;&nbsp;@Html.DisplayNameFor(model => model.Polygon)
                    @if (Element.Required == true)
                    {
                        <text>*</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }
                </td>
                <td>
                    <div id="elementInput">

                        @if (Element.DatatypeSize < 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.Polygon) value
                                <input type="text" name="elementValue" value="@Html.DisplayFor(modelItem => item.Polygon)" size="25" />
                            </span>
                        }
                        else if (Element.DatatypeSize > 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.Polygon) value
                                <textarea name="elementValue" rows="3" value="@Html.DisplayFor(modelItem => item.Polygon)" cols="25">
                                </textarea>
                            </span>
                        }
                    </div>
                </td>
                <td>
                    @if (Element.Repeatable == true)
                    {
                        <input type="radio" name="repeatElement" value="" /><text>&nbsp; repeat</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }    <input type="radio" name="repeatElement" value="" /><text>&nbsp; repeat</text>
                </td>
                <td>
                    <form asp-area="" asp-controller="areas" asp-action="Details" method="post" asp-route-id="@item.AreaIndex">
                        <input type="hidden"
                               name="elementId"
                               value="${element.id}">
                        <input type="submit"
                               value="add element">
                    </form>
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.AreaIndex">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.AreaIndex">Delete</a>
                </td>
            </tr>
            <tr>
                <td>
                    <input type="checkbox" name="start">start
                </td>
                <td>
                    &nbsp;&nbsp;element:<br>
                    &nbsp;&nbsp;@Html.DisplayNameFor(model => model.Circle)
                    @if (Element.Required == true)
                    {
                        <text>*</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }
                </td>
                <td>
                    <div id="elementInput">

                        @if (Element.DatatypeSize < 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.Circle) value
                                <input type="text" name="elementValue" value="@Html.DisplayFor(modelItem => item.Circle)" size="25" />
                            </span>
                        }
                        else if (Element.DatatypeSize > 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.Circle) value
                                <textarea name="elementValue" rows="3" value="@Html.DisplayFor(modelItem => item.Circle)" cols="25">
                                </textarea>
                            </span>
                        }
                    </div>
                </td>
                <td>
                    @if (Element.Repeatable == true)
                    {
                        <input type="radio" name="repeatElement" value="" /><text>&nbsp; repeat</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }    <input type="radio" name="repeatElement" value="" /><text>&nbsp; repeat</text>
                </td>
                <td>
                    <form asp-area="" asp-controller="areas" asp-action="Details" method="post" asp-route-id="@item.AreaIndex">
                        <input type="hidden"
                               name="elementId"
                               value="${element.id}">
                        <input type="submit"
                               value="add element">
                    </form>
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.AreaIndex">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.AreaIndex">Delete</a>
                </td>
            </tr>
            <tr>
                <td>
                    <input type="checkbox" name="start">start
                </td>
                <td>
                    &nbsp;&nbsp;element:<br>
                    &nbsp;&nbsp;@Html.DisplayNameFor(model => model.Geocode)
                    @if (Element.Required == true)
                    {
                        <text>*</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }
                </td>
                <td>
                    <div id="elementInput">

                        @if (Element.DatatypeSize < 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.Geocode) value
                                <input type="text" name="elementValue" value="@Html.DisplayFor(modelItem => item.Geocode)" size="25" />
                            </span>
                        }
                        else if (Element.DatatypeSize > 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.Geocode) value
                                <textarea name="elementValue" rows="3" value="@Html.DisplayFor(modelItem => item.Geocode)" cols="25">
                                </textarea>
                            </span>
                        }
                    </div>
                </td>
                <td>
                    @if (Element.Repeatable == true)
                    {
                        <input type="radio" name="repeatElement" value="" /><text>&nbsp; repeat</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }    <input type="radio" name="repeatElement" value="" /><text>&nbsp; repeat</text>
                </td>
                <td>
                    <form asp-area="" asp-controller="areas" asp-action="Details" method="post" asp-route-id="@item.AreaIndex">
                        <input type="hidden"
                               name="elementId"
                               value="${element.id}">
                        <input type="submit"
                               value="add element">
                    </form>
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.AreaIndex">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.AreaIndex">Delete</a>
                </td>
            </tr>
            <tr>
                <td>
                    <input type="checkbox" name="start">start
                </td>
                <td>
                    &nbsp;&nbsp;element:<br>
                    &nbsp;&nbsp;@Html.DisplayNameFor(model => model.Altitude)
                    @if (Element.Required == true)
                    {
                        <text>*</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }
                </td>
                <td>
                    <div id="elementInput">

                        @if (Element.DatatypeSize < 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.Altitude) value
                                <input type="text" name="elementValue" value="@Html.DisplayFor(modelItem => item.Altitude)" size="25" />
                            </span>
                        }
                        else if (Element.DatatypeSize > 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.Altitude) value
                                <textarea name="elementValue" rows="3" value="@Html.DisplayFor(modelItem => item.Altitude)" cols="25">
                                </textarea>
                            </span>
                        }
                    </div>
                </td>
                <td>
                    @if (Element.Repeatable == true)
                    {
                        <input type="radio" name="repeatElement" value="" /><text>&nbsp; repeat</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }
                </td>
                <td>
                    <form asp-area="" asp-controller="areas" asp-action="Details" method="post" asp-route-id="@item.AreaIndex">
                        <input type="hidden"
                               name="elementId"
                               value="${element.id}">
                        <input type="submit"
                               value="add element">
                    </form>
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.AreaIndex">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.AreaIndex">Delete</a>
                </td>
            </tr>
            <tr>
                <td>
                    <input type="checkbox" name="start">start
                </td>
                <td>
                    &nbsp;&nbsp;element:<br>
                    &nbsp;&nbsp;@Html.DisplayNameFor(model => model.Ceiling)
                    @if (Element.Required == true)
                    {
                        <text>*</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }
                </td>
                <td>
                    <div id="elementInput">

                        @if (Element.DatatypeSize < 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.Ceiling) value
                                <input type="text" name="elementValue" value="@Html.DisplayFor(modelItem => item.Ceiling)" size="25" />
                            </span>
                        }
                        else if (Element.DatatypeSize > 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.Ceiling) value
                                <textarea name="elementValue" rows="3" value="@Html.DisplayFor(modelItem => item.Ceiling)" cols="25">
                                    </textarea>
                            </span>
                        }
                    </div>
                </td>
                <td>
                    @if (Element.Repeatable == true)
                    {
                        <input type="radio" name="repeatElement" value="" /><text>&nbsp; repeat</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }
                </td>
                <td>
                    <form asp-area="" asp-controller="areas" asp-action="Details" method="post" asp-route-id="@item.AreaIndex">
                        <input type="hidden"
                               name="elementId"
                               value="${element.id}">
                        <input type="submit"
                               value="add element">
                    </form>
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.AreaIndex">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.AreaIndex">Delete</a>
                </td>
            </tr>
            <tr>
                <td>
                    <input type="checkbox" name="start">start
                </td>
                <td>
                    &nbsp;&nbsp;element:<br>
                    &nbsp;&nbsp;@Html.DisplayNameFor(model => model.DataCategory_Id)
                    @if (Element.Required == true)
                    {
                        <text>*</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }
                </td>
                <td>
                    <div id="elementInput">

                        @if (Element.DatatypeSize < 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.DataCategory_Id) value
                                <input type="text" name="elementValue" value="@Html.DisplayFor(modelItem => item.DataCategory_Id)" size="25" />
                            </span>
                        }
                        else if (Element.DatatypeSize > 49)
                        {
                            <span class="smallText">
                                Enter @Html.DisplayNameFor(model => model.DataCategory_Id) value
                                <textarea name="elementValue" rows="3" value="@Html.DisplayFor(modelItem => item.DataCategory_Id)" cols="25">
                                    </textarea>
                            </span>
                        }
                    </div>
                </td>
                <td>
                    @if (Element.Repeatable == true)
                    {
                        <input type="radio" name="repeatElement" value="" /><text>&nbsp; repeat</text>
                    }
                    else
                    {
                        <text>&nbsp;&nbsp;</text>
                    }
                </td>
                <td>
                    <form asp-area="" asp-controller="areas" asp-action="Details" method="post" asp-route-id="@item.AreaIndex">
                        <input type="hidden"
                               name="elementId"
                               value="${element.id}">
                        <input type="submit"
                               value="add element">
                    </form>
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.AreaIndex">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.AreaIndex">Delete</a>
                </td>
            </tr>

        }

    </table>

</body>


<p style="font-size:x-small;padding:2px;text-align:center;color:#ffffff">* = required &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; repeat button is for multiple-use elements. </p>

</html>


